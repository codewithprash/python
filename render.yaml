# render.yaml

services:
  - name: my-flask-app
    env:
      - key: PORT
        value: 8080  # Specify the port your app should listen on
    startCommand: python app.py  # The command to start your app
